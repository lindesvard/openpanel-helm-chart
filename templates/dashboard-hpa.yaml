{{- if and .Values.dashboard.enabled .Values.dashboard.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.dashboard.service.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: op-dashboard
    component: dashboard
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.dashboard.service.name }}
  minReplicas: {{ .Values.dashboard.hpa.minReplicas }}
  maxReplicas: {{ .Values.dashboard.hpa.maxReplicas }}
  metrics:
{{- if .Values.dashboard.hpa.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.dashboard.hpa.targetCPUUtilizationPercentage }}
{{- end }}
{{- if .Values.dashboard.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.dashboard.hpa.targetMemoryUtilizationPercentage }}
{{- end }}
{{- end }}
